<%= form_with(model: [stroll, comment]) do |form| %>
  <div class="field">
    <%= form.text_area :content, id: "comment_area" %>
  </div>
  <div class="actions">
    <%= form.submit "コメントする" %>
  </div>
<% end %>
